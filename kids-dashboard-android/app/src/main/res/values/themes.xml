<resources>
    <!-- 액션바 없는 전체화면 테마 (AppCompatActivity 기반) -->
    <style name="Theme.KidsKiosk" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/brand_blue</item>
        <item name="android:windowBackground">@color/brand_blue</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
